---
description: Simplify local code using the simplify-code skill
---

Use the `simplify-code` skill to simplify local code. Scope argument: `$ARGUMENTS` (one of `branch` | `staged` | `working` | one or more paths; default `branch` if empty). Follow the skill's full pipeline — do not skip the contract extraction, validation, or scoring steps, even under time pressure.
